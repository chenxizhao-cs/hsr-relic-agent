# AGENTS.md

## 项目简介

这是清华大学 Rust 课程的 AI Agent 大作业项目。

项目暂定名称：

**《崩坏：星穹铁道》遗器管理与强化决策 Agent**

项目目标是：根据玩家的账号状态、目标角色、当前配装和有限强化资源，帮助玩家管理大量遗器，并判断下一份强化资源应该投入到哪里。

本项目可以调用、集成或参考已有的开源《崩坏：星穹铁道》社区工具，不要求重复实现已经成熟的功能。

---

## 核心设计原则

- 项目的核心业务逻辑、状态管理、工具编排和 API 调用流程必须使用 **Rust** 实现。
- 遗器评分、属性计算、配装搜索、资源计算等确定性任务，应优先交给确定性程序或工具处理，而不是让大模型直接计算。
- LLM Agent 主要负责：
  - 理解用户的自然语言目标；
  - 提取约束和偏好；
  - 选择和编排工具；
  - 制定和调整计划；
  - 解释结果。
- 优先复用可靠的开源工具、公开数据格式、接口和数学模型，避免无意义地重复造轮子。
- 除非当前任务明确要求，否则不要主动扩大项目范围。

---

## 课程硬性要求

最终项目必须完整实现以下六项要求：

1. **Rust 核心逻辑**
   - 核心业务逻辑、数据处理、算法流程和 API 调用编排必须使用 Rust。

2. **用户交互界面**
   - 至少提供 Web、CLI、桌面 App 或手机 App 中的一种。

3. **可配置大模型**
   - 用户可以配置：
     - API Endpoint
     - API Key
     - 上下文长度
     - 思考模式
     - API 价格等参数

4. **实时进度与打断**
   - 执行时间超过 3 秒的任务必须显示实时进度。
   - 用户必须可以中断任务。

5. **历史与上下文管理**
   - 支持查看历史任务或对话。
   - 支持保存和加载完整上下文与 Agent 执行轨迹。

6. **Token 与费用统计**
   - 精确记录每次 API 调用的输入 Token 和输出 Token。
   - 根据配置价格计算费用。
   - 在界面中展示统计信息。
   - 支持设置 Token 预算并在达到预算时中断。

课程要求：

https://lab.cs.tsinghua.edu.cn/rust/projects/agent/requirements/

---

## 仓库约定

- `upstream/` 目录用于存放第三方开源项目，仅用于调研、调用或集成。
- **不要修改 `upstream/` 中的第三方项目。**
- 在自行实现某项功能之前，先检查现有开源项目是否已经提供合适的接口或能力。
- 我们自己的代码与第三方项目保持清晰分离。
- 不要在源码、文档或 Git 历史中写入 API Key、Token、密码等敏感信息。

---

## 开源代码与 Honor Code

- 如果需要大篇幅复制第三方代码，请在对话里说明，并在取得许可后再做。
- 优先采用以下方式复用已有成果：
  - 调用公开接口；
  - 兼容公开数据格式或协议；
  - 使用开源库或独立程序；
- 所有重要的外部项目、数据和算法来源都应记录在设计文档中。
- 如果确实复制或修改了第三方代码，必须按照课程要求标注：
